require 'spec_helper'

describe MysqlQueryTimeout do
  it 'has a version number' do
    expect(MysqlQueryTimeout::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
